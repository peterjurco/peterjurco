# Scheduled Neon → R2 database backup (TECH_DECISIONS §4): pg_dump can't run
# on the Workers runtime, so the dump runs here — see scripts/backup-db.sh.
#
# Required repository secrets:
#   DATABASE_URL                  Neon connection string
#   R2_BACKUP_ENDPOINT_URL        https://<account-id>.r2.cloudflarestorage.com
#   R2_BACKUP_BUCKET              destination bucket
#   R2_BACKUP_ACCESS_KEY_ID       R2 S3-API credentials
#   R2_BACKUP_SECRET_ACCESS_KEY
name: DB backup

on:
  schedule:
    # Daily, 03:17 UTC — an off-peak minute; on-the-hour crons get delayed
    # or dropped when GitHub's schedulers are busy.
    - cron: "17 3 * * *"
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # pg_dump can't dump from a server NEWER than itself, so the client
      # major version must be >= the actual Neon server's (confirmed 18.4
      # in production as of 2026-07 — Neon defaults new projects ahead of
      # docker-compose.test.yml's local Postgres 17, which is fine: pg_dump
      # 18 dumps older servers fine, and the local stack only ever exercises
      # a STUBBED pg_dump in tests, never the real binary). Install from
      # PGDG since the runner's default client lags behind.
      - name: Install postgresql-client-18
        run: |
          sudo install -d /usr/share/postgresql-common/pgdg
          sudo curl -fsSL -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc \
            https://www.postgresql.org/media/keys/ACCC4CF8.asc
          echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" \
            | sudo tee /etc/apt/sources.list.d/pgdg.list
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends postgresql-client-18

      - name: Dump, upload, prune
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          R2_BACKUP_ENDPOINT_URL: ${{ secrets.R2_BACKUP_ENDPOINT_URL }}
          R2_BACKUP_BUCKET: ${{ secrets.R2_BACKUP_BUCKET }}
          R2_BACKUP_ACCESS_KEY_ID: ${{ secrets.R2_BACKUP_ACCESS_KEY_ID }}
          R2_BACKUP_SECRET_ACCESS_KEY: ${{ secrets.R2_BACKUP_SECRET_ACCESS_KEY }}
        run: ./scripts/backup-db.sh
