---
import { AlbumForm } from '../../../components/AlbumForm'
import AppNav from '../../../components/AppNav.astro'
import BaseLayout from '../../../layouts/BaseLayout.astro'

// Defense in depth: the middleware already gates /app/*.
if (!Astro.locals.user) {
  return Astro.redirect('/api/auth/login', 302)
}
---

<BaseLayout title="Add album — peterjur.co">
  <AppNav />
  <main class="admin-shell album-page">
    <h1>Add album</h1>
    <AlbumForm client:load />
    <p><a href="/app/photos">← Photo hub</a></p>
  </main>
</BaseLayout>
